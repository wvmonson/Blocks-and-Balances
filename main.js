class Block {
    constructor (index, timestamp, date, previousHash = '' ){
    this.index = index;
    this.timestamp = timestamp;
    this.data = data;
    this.previousHash = previousHash;
    this.hash = '';
    }
    calculateHaash(){
        
    }
}
